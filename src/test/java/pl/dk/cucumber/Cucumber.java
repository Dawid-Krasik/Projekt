package pl.dk.cucumber;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

public class Cucumber {
    public static void main(String[] args) {
        WebDriver driver = new FirefoxDriver();
    }
}
